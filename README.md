# Humorithm

```bash
git clone https://github.com/0x7o/humorithm
cd humorithm
docker-compose up --build
```

- `POST /api/v1/predict` - генерация мема
- `GET /api/v1/stats/templates` - получение статистики по шаблонам
- `GET /output/<filename>` - получение сгенерированного изображения
